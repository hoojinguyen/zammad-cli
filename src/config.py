# Zammad CLI Centralized Configurations

# Official Zammad Docker Compose git repository URL to clone at runtime
ZAMMAD_COMPOSE_REPO = "https://github.com/zammad/zammad-docker-compose.git"

# Default host port mapping for Zammad Nginx proxy web interface
DEFAULT_WEB_PORT = 8080

# Default PostgreSQL database settings
DB_DEFAULT_USER = "zammad_rw"
DB_DEFAULT_NAME = "zammad"

# Elasticsearch System requirements for Linux kernel parameters
REQUIRED_VM_MAX_MAP_COUNT = 262144
